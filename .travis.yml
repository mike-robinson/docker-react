sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mrobinson108/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mrobinson108/docker-react npm run test
